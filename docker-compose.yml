version: '3.8'

services:
  # Using Supabase instead of local postgres
  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_DB: livecommerce
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  ml-service:
    build: ./ml_service
    ports:
      - "7001:8001"
    volumes:
      - ./datasets:/app/datasets
      - ./embeddings:/app/embeddings
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1

  backend:
    build: ./backend
    ports:
      - "7080:8080"
    depends_on:
      - ml-service
    environment:
      - DATABASE_URL=postgresql://postgres.aoefomhdatzkcsuowqmb:DFSiOWif2PCjd4X3@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require
      - SUPABASE_URL=https://aoefomhdatzkcsuowqmb.supabase.co
      - SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFvZWZvbWhkYXR6a2NzdW93cW1iIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjE1Mzg0MjQsImV4cCI6MjA3NzExNDQyNH0.79iinbsH4HWkb5YMrK_CAxnKMVlYZfQXzfLpapaLxZo
      - SUPABASE_STORAGE_BUCKET=products
      - ML_SERVICE_URL=http://ml-service:8001

  frontend:
    build: ./frontend
    ports:
      - "7000:3000"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:7080
      - VITE_WS_URL=ws://localhost:7080

volumes:
  postgres_data: